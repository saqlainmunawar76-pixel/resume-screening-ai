streamlit==1.58.0
pdfplumber==0.11.9
pypdf==6.4.0
scikit-learn==1.8.0
pandas
numpy
python-dotenv==1.1.1
google-genai==2.9.0

# Optional: install this for richer semantic similarity (transformer embeddings)
# instead of the built-in TF-IDF fallback. Requires internet access to
# download the model on first run.
# sentence-transformers==3.0.1
